package Chats.Controller;

import Chats.Model.PV;
import Chats.Model.newChat;

public class newChatController {
    public newChatController(newChat newChat) {
    }

    public PV createNewChat(String userUUID) {
        return new PV();
    }
}
